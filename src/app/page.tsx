// src/app/page.tsx
// This file simply serves as a fallback if redirects ever fail.
// It can safely remain minimal since the redirect is now handled in next.config.ts.

export default function HomePage() {
  return (
    <main className="flex items-center justify-center min-h-screen">
      <p>Redirecting...</p>
    </main>
  );
}
